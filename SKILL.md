---
name: readable-maintainable-code
description: 为生成或编辑过的代码添加面向维护的注释，并在修改后提供说明；尤其适用于 vibe coding 工作流。
---

# 目的

帮助 Codex 生成更容易被非专家或半专家开发者理解、修改和后续维护的代码。

# 触发提示

当出现以下情况时，应启用本技能：

- 用户输入 `/🫪`
- 用户要求代码更易读或更易维护
- 用户提到“解释代码”
- 用户使用如下标记：
  - `-s`：简单模式
  - `-d`：详细模式
  - `-sum`：生成总结
- 这些标记可能出现在用户提示词中
- 用户指定了本技能应作用的文件范围

# 主要规则

## 1. 为维护而注释，不要复述代码

不要解释显而易见的语法。

不好的注释：

- 递增 count
- 创建数组
- 遍历 items

好的注释应解释：

- 意图
- 设计取舍
- 不变量
- 边界情况
- 未来修改点
- 副作用

## 2. 优先使用块级说明，避免逐行噪音

对于直观的代码，不要添加注释。

对于复杂片段，在代码块之前添加简短的块级注释。

## 3. 说明修改热点

尽可能标记：

- 未来功能变化最可能发生的位置
- 哪些逻辑可以安全修改
- 代码的其他部分依赖这里的哪些行为

## 3.5 为非平凡文件生成项目流程图

当源文件包含多个函数、组件或异步路径时，在 Markdown 输出中额外包含项目流程图或代码思维导图。

- 优先使用 Mermaid `flowchart` 语法。
- 使用函数、视图、任务或其他重要组件作为节点。
- 节点描述要具体，并贴合当前代码上下文。
- 说明是什么触发该节点、它调用或更新了什么、接下来会发生什么。
- 如果节点代表 UI 操作，要写明它会打开的具体 sheet、导航目标、alert 或 task。
- 如果节点代表数据加载或异步工作，要写明数据来源、取消点、主线程切换位置，以及它会写入的下游状态。
- 把说明嵌入节点内或紧挨节点放置，使图本身可以直接阅读，不需要再翻找单独的说明段落。
- 如果需要在浏览器中查看，并且需要点击节点跳转到源码行，则生成一个 HTML companion。
- 单文件分析时，HTML companion 应在节点详情区域渲染完整原始源文件，并在每一行开头显示编译器风格的行号。
- 多文件或项目分析时，HTML companion 应在节点详情区域渲染源码摘录，同样在每一行开头显示编译器风格的行号。
- Mermaid 的 `click` 目标和索引表链接应指向 HTML 行锚点，保证点击节点或索引项时跳转到同一源码行。

## 4. 保持注释简洁

每条注释都必须值得存在。

避免在代码中写冗长说明。

## 5. 完成代码修改、注释和说明后，提供开发者总结

开发者总结的内容来自 `summary.md`。

总结是可选项，只应在用户用自然语言明确要求，或使用 `-sum` 命令时生成。

每个生成的 `summary.md` 都必须以创建时的准确生成日期和时间开头，格式为 `YYYY-MM-DD HH:MM:SS TZ`。

总结始终应包含：

- 生成日期和时间，格式为 `YYYY-MM-DD HH:MM:SS TZ`
- 修改了什么
- 为什么这样实现
- 之后可以安全修改什么
- 修改什么比较有风险
- 所有假设或约束
- 建议的下一步改进

# 模式

除非用户明确要求简单模式，否则默认使用详细模式。

## 简单模式（`-s`）

- 只添加最少量的注释和说明
- 只解释关键内容
- 总结保持简短

## 详细模式（`-d`）

- 提供完整注释和说明
- 包含设计理由
- 包含修改指导

# 语言特定指导

## Swift / SwiftUI

- 适合时为面向外部使用的 API 添加文档注释
- 说明状态所有权和数据流
- 解释不明显的动画或布局决策
- 避免注释琐碎的 view modifier
- 突出 binding、environment 依赖，以及异步/状态交互

## C / Embedded

- 说明寄存器假设、时序要求、状态转换和位级含义
- 标出硬件依赖和副作用
- 标记必须与 datasheet 或引脚配置保持同步的代码

# 修改后的输出格式

## 文件放置规则

用户必须先指定本技能应作用的文件范围，然后才能生成输出。

按用途和位置拆分输出：

- `comment` 和 `explanation` 必须作为代码注释写入原始源文件
- 如果语言和文件风格允许，文件级说明应放在头部注释中
- 针对具体代码的说明应放在相关函数、类或逻辑块附近，作为局部注释
- 不要把 `comment` 或 `explanation` 放在单独的报告文件中
- 当用户要求项目流程图、代码思维导图或结构概览时，创建一个 Markdown 文件，其中同时包含说明文字和 Mermaid 图；节点文本必须引用具体调用方、被调用方、目标位置和副作用
- 如果用户要求为单个文件生成图表，并且需要单独文件，则将其放在名为 `chart_YYYYMMDD_HHMMSS.md` 的图表文件中
- 图表是必需的：只要用户提供文件范围，就始终为该范围生成图表
- 如果用户要求为多个文件或整个项目生成图表，则创建名为 `chart_YYYYMMDD_HHMMSS.md` 的图表文件
- 当用户要求浏览器查看时，还要创建匹配的 HTML companion，供 Markdown 图表链接到精确行号
- 单文件模式下，HTML companion 应显示完整源文件作为节点详情区域，而不是缩短后的代码块
- 多文件或项目模式下，HTML companion 应在节点详情区域显示源码摘录，而不是完整文件转储
- 两种模式下，每一行源码开头都应带有可见行号，所有链接都应使用这些行锚点
- 如果用户要求为单个文件生成总结，优先把总结追加到该文件底部，而不是创建单独的总结文件
- 如果用户要求为多个文件或项目生成总结，则将其放在名为 `summary_YYYYMMDD_HHMMSS.md` 的文件中
- 所有单独的总结或图表文件，第一行都必须是准确的生成日期和时间，格式为 `YYYY-MM-DD HH:MM:SS TZ`
- 不要在源文件底部追加游离的、非注释形式的说明
